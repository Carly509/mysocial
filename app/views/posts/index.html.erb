<div class="max-w-3xl mx-auto">
  <div class="mb-8">
    <%= render "form", post: @post %>
  </div>

  <div class="space-y-6">
    <%= render @posts %>
  </div>

  <div class="mt-6">
    <%= paginate @posts %>
  </div>
</div>
